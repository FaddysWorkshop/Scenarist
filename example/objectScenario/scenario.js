import { createInterface } from 'readline/promises';
import { stdin as input, stdout as output } from 'process';

class Order {

$drink ( play, drink ) {

cli .question ( `${ question }

${ answers .map ( ( answer, index ) => `${ index + 1 }. ${ answer }

}

}

class Waiter {

cli = createInterface ( { input, output } )

async $_ask () {

const waiter = this;
const { cli, question, answers } = waiter;

waiter .answer = await cli .question ( `${ question }

${ answers .map ( ( answer, index ) => `${ index + 1 }. ${ answer }` ) .join ( '\n' ) }

Your answer: `;

}

};
